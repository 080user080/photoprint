# TODO: Undo/Redo, інструмент кадрування А4, налаштування autofix

## 1. Undo/Redo (глобальна історія редагування файлу)
- [ ] Спроєктувати стек історії дій для поточного відкритого файлу: кожен крок (crop, перспектива, autofix, ручні слайдери, shadow remove, deskew, скидання) записує знімок стану (або diff) `_base`/`_processed` + пов'язані `_per_file` налаштування
- [ ] Історія тримається, поки файл не відправлено на друк (`_do_print_current` / `_do_print_all` / завершення авто-режиму) — після друку стек очищується
- [ ] Історія кожного файлу тримається "на паузі" окремо (прив'язана до шляху файлу, за аналогією з існуючим `_per_file`): перехід на інший файл у черзі не скидає історію попереднього, повернення — відновлює її
- [ ] Додати кнопки "◀ Назад" / "Вперед ▶" на панель керування (біля кнопок деskew)
- [ ] Додати гарячі клавіші Ctrl+Z (undo) / Ctrl+Y або Ctrl+Shift+Z (redo), працюють тільки коли фокус у головному вікні
- [ ] Інтегрувати запис кроку в історію в усіх точках коміту: `_do_autofix_classic._on_done`, `_do_auto_brightness/_contrast/_sharpen`, коміт перспективи (ручної/авто/hover-кадрування), `_commit_deskew`, `_do_persp_reset`, `_do_reset_all`, зміни слайдерів (дебaunс-коміт)
- [ ] Продумати обмеження пам'яті: історія на кожен файл окремо + кілька файлів у черзі одночасно "на паузі" — потенційно багато повних знімків зображень у пам'яті; визначити ліміт кроків історії на файл або механізм очищення історії файлів, які давно не відкривались
- [ ] Ручне тестування: 10+ послідовних дій, undo/redo в різних напрямках; перемикання між кількома файлами в черзі з незавершеною історією в кожному, перевірка коректного відновлення при поверненні; перевірка на розсинхрон preview і на споживання пам'яті

## 2. Новий hover-інструмент "Кадрування А4" (паралельно з існуючими кнопками перспективи)
- [ ] Реалізувати hover-активацію: при наведенні курсора на зображення "ДО" (оригінал/поточний `_base`) — без кліку з'являється рамка кадрування з 4 кутовими точками + іконка кадрування + жирний червоний напис у зоні прев'ю "Інструмент кадрування"
- [ ] При відведенні курсора з зображення — оверлей і напис зникають, якщо кадрування ще не підтверджено; якщо користувач вже почав тягнути точку — оверлей залишається до explicit confirm/cancel
- [ ] Реалізувати "розумну" логіку визначення орієнтації А4 за формою поточного виділення:
  - ширина виділення > висоти → пропорції А4 альбомні (landscape)
  - висота ≥ ширини, включно з квадратом і близькими до квадрата пропорціями → А4 книжкові (portrait)
- [ ] Кутові точки виконують одночасно роль crop-рамки (прямокутне обрізання по краю) і перспективної корекції (перетягування кута = зміна перспективи, результат одразу кадрується по знайдених межах)
- [ ] Додати явні кнопки "Застосувати кадрування" / "Скасувати" (за зразком уже наявних кнопок deskew), щоб коміт відбувався тільки за явним підтвердженням користувача, а не неявно
- [ ] Цей інструмент існує паралельно з наявними кнопками "Авто-перспектива" та "Ручна перспектива" — жодна з них не видаляється і не змінюється в цьому пункті
- [ ] Продумати, як цей новий інструмент співіснує з наявним `edit_mode` в `ImageLabel` (gui/preview.py), який зараз використовується кнопками перспективи — уникнути конфлікту двох одночасно активних режимів редагування точок на одному зображенні
- [ ] Ручне тестування на фото різних пропорцій (портрет, альбом, квадрат) — перевірка правильності обраної орієнтації А4; перевірка, що ручна/авто-перспектива через старі кнопки продовжують працювати як раніше

## 3. Налаштування після кадрування (per-file, тимчасові)
- [ ] Одразу після підтвердження кадрування (кнопка "Застосувати кадрування" з пункту 2) — показати міні-панель вибору кроків обробки для застосування саме до цього кадрованого файлу
- [ ] Список опцій = міні-версія `pipeline_steps_enabled` (перспектива вже виконана кадруванням, тому в цьому списку не дублюється; залишаються: autofix у цілому або по кроках — shadow_remove, auto-яскравість, auto-контраст, HDR, sharpen, grayscale, white_background)
- [ ] Вибір застосовується одноразово до кадрованого зображення поточного файлу (зберігається в `_per_file`, не міняє глобальні `settings.ini`)
- [ ] Ручне тестування: кадрування → вибір підмножини кроків → перевірка, що фінальний результат використовує тільки вибрані кроки

## 4. Покращення автоперспективи (детекція документа) — напрямок роботи
- [ ] Наразі немає доступу до перегляду конкретних проблемних фото в `tests/test_images/` в режимі обговорення — цей пункт лишається загальним напрямком, без точних порогових значень, поки агент кодування не відкриє файли напряму
- [ ] Агенту: переглянути наявні фото в `tests/test_images/`, зафіксувати конкретні приклади, де детекція:
  - обрізає лише частину тексту замість всього документа
  - взагалі не знаходить документ
- [ ] Для кожного проблемного прикладу визначити, який метод детекції "виграв" скоринг (`_try_adaptive_threshold` / `_try_canny` / `_try_external_contour` / Hough) — додати тимчасове логування скорів усіх кандидатів для діагностики
- [ ] На основі знайдених прикладів скоригувати `_score_quad` так, щоб зовнішній контур документа пріоритетувався над внутрішнім контрастним блоком (текст/таблиця/абзац)
- [ ] За результатами — переглянути конкретні пороги (`MIN_DOCUMENT_AREA_RATIO`, `MAX_QUAD_AREA_RATIO`, `PARTIAL_SKEW_THRESHOLD_RATIO` тощо); точні нові значення визначаються тільки експериментально на реальних прикладах
- [ ] Додати ці проблемні фото як постійний регресійний набір, щоб майбутні зміни детекції перевірялись на них автоматично
- [ ] Ручне тестування: порівняти результат детекції до/після на всьому наборі

## 5. Виправлення бага ручної перспективи (стара кнопка "Ручна перспектива")
- [ ] Причина: `_do_persp_manual` не має явного commit/cancel — при повторному натисканні кнопки поточні (можливо ще не відредаговані користувачем) точки одразу комітяться в `_base` через `pipeline.run_perspective_manual`, а потім виставляються нові дефолтні точки — звідси ефект "замінює оригінал на стан де стояли точки і ставить нові"
- [ ] Оскільки стара кнопка залишається (рішення з пункту 2 — паралельне співіснування), виправити саме її: прибрати неявний коміт при повторному натисканні кнопки "Ручна перспектива"
- [ ] Додати явні кнопки "Застосувати"/"Скасувати" для цього режиму (за аналогією з уже наявними кнопками deskew), щоб коміт у `_base` відбувався тільки за явною дією користувача
- [ ] Узгодити ці кнопки з новим hover-інструментом кадрування (пункт 2) — уникнути дублювання UI-елементів "Застосувати"/"Скасувати", якщо обидва інструменти активні по черзі
- [ ] Ручне тестування: багаторазове натискання кнопки "Ручна перспектива" на одному файлі без явного підтвердження — оригінал/`_base` не повинні змінюватись до натискання "Застосувати"

## 6. Autofix — додати "Авто-яскравість" як окремий пункт
- [ ] У вікні налаштувань, розділ "Auto Fix" — додати чекбокс "Авто-яскравість" (наразі в `run_autofix` крок `"brightness"` — це no-op: "Яскравість окремо не застосовується в autofix — вона в ручних налаштуваннях")
- [ ] Реалізувати сам крок: якщо чекбокс увімкнено — викликати `bc.auto_brightness()` в кроці `"brightness"` пайплайну `run_autofix`, аналогічно тому, як зараз реалізовано кнопку "Авто яскравість" в ручному режимі
- [ ] Параметри перцентилів для цього кроку — брати з уже наявних налаштувань `auto_percentile_low` / `auto_percentile_high` (сторінка "Яскравість/контраст" у налаштуваннях), без дублювання нових полів
- [ ] Додати новий чекбокс у `pipeline_steps_enabled` (кастомний пресет) та у відповідні пресети (`doc_bw`, `doc_color`, `photo`) — вирішити, чи вмикати його там за замовчуванням, чи лишити вимкненим доки користувач не увімкне вручну
- [ ] Ручне тестування: увімкнути/вимкнути авто-яскравість у autofix, порівняти результат на чб та кольорових документах